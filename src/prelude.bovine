
!{
  print,
  println,
  printf,
} = import '@io';

!{
  not,
  range,
} = import '@basics';

!{
  push,
  pop,
  len,
  sort,
  map,
  filter,
} = import '@list';

! {
  removeKey,
} = import '@dict';
