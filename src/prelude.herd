
!{
  print,
  println,
  printf,
} = import '@io';

!{
  not,
  range,
  assert,
} = import '@basics';

!{
  push,
  pop,
  len,
  map,
  filter,
} = import '@list';

! {
  removeKey,
} = import '@dict';
