
!{
  not,
  range,
  assert,
} = import '@basics';

IO = import '@io';

!{
  print,
  println,
  printf,
} = IO;

List = import '@list';

!{
  push,
  pop,
  len,
  map,
  filter,
} = List;

Dict = import '@dict';

Math = import '@math';

Bitwise = import '@bitwise';

Random = import '@random';
