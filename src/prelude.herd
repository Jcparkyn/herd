
!{
  not,
  range,
  assert,
  toString,
} = import '@basics';

System = import '@system';

IO = import '@io';

!{
  print,
  println,
  printf,
} = import '@io';

List = import '@list';

!{
  push,
  pop,
  len,
  map,
  filter,
} = List;

Dict = import '@dict';

Math = import '@math';

Bitwise = import '@bitwise';

Random = import '@random';

String = import '@string';

File = import '@file';
