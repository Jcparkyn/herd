
!{
  not,
  range,
  assert,
} = import '@basics';

System = import '@system';

IO = import '@io';

!{
  print,
  println,
  printf,
} = import '@io';

List = import '@list';

!{
  push,
  pop,
  len,
  map,
  filter,
} = import '@list';

Dict = import '@dict';

Math = import '@math';

Bitwise = import '@bitwise';

Random = import '@random';

String = import '@string';
