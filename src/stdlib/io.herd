
print = \..val\ (
  for x in val do (
    BUILTIN:print x;
  )
);

println = \..val\ (
  for x in val do (
    BUILTIN:print x;
  )
  BUILTIN:print '\n';
);

readline = \\ (
  BUILTIN:readline
);

return {
  print,
  println,
  readline,
};
