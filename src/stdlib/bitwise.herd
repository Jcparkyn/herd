
shiftLeft = \a by\ BUILTIN:shiftLeft a by;

shiftRight = \a by\ BUILTIN:shiftLeft a (0 - by);

xor = \a b\ BUILTIN:bitwiseXor a b;

bitwiseAnd = \a b\ BUILTIN:bitwiseAnd a b;

bitwiseOr = \a b\ BUILTIN:bitwiseOr a b;

return {
  shiftLeft,
  shiftRight,
  xor,
  bitwiseAnd,
  bitwiseOr,
};
