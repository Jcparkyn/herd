isEven_ = \isOdd n\ (
  if n == 0 then true else (isOdd (n - 1))
);

isOdd = \n\ (
  if n == 0 then false else (isEven_ isOdd (n - 1))
);

isEven = \n\ isEven_ isOdd n;

println (isEven 0);
println (isEven 1);
println (isEven 2);
println (isEven 3);

println (isOdd 0);
println (isOdd 1);
println (isOdd 2);
println (isOdd 3);
